fun main() {
    val (n, q) = readln().split(" ").map { it.toInt() }
    val a = readln().split(" ").map { it.toInt() }


    for (i in 0 until q) {
        val (t, x, y) = readln().split(" ").map { it.toInt() }

        if (t == 1) {

        }
        if (t == 2) {

        }
        if (t == 3) {

        }
    }
}
